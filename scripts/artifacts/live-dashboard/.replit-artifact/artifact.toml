kind = "web"
previewPath = "/"
title = "Live Stock Dashboard"
version = "1.0.0"
id = "artifacts/live-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22491

[services.development]
run = "pnpm --filter @workspace/live-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/live-dashboard", "run", "build" ]
publicDir = "artifacts/live-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22491"
BASE_PATH = "/"
