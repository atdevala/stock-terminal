Fix a critical missing feature in my stock signal system:

I currently calculate:
- VQS
- GVS
- COS
- INS
- ACS

BUT I DO NOT TRACK HOW THESE CHANGE OVER TIME.

This is required to make the system usable.

---

# 1. CORE REQUIREMENT

Add full TIME SERIES tracking for ALL signals:

- VQS history
- GVS history
- COS history
- INS history
- ACS history

Must support:
- daily tracking
- optional intraday (hourly if data available)

---

# 2. STORE SIGNAL HISTORY

Create a persistent storage system:

For each ticker store:

{
  ticker,
  timestamp,
  VQS,
  GVS,
  COS,
  INS,
  ACS
}

Save a new snapshot every time the system runs.

Minimum retention:
- 30 days (required)
- 90 days (preferred)

---

# 3. CREATE SIGNAL DELTA ENGINE

For each signal compute:

--------------------------------------------------
A. DAILY CHANGE
--------------------------------------------------

Delta_1D:
- current_value - previous_day_value

Apply to:
- VQS
- GVS
- COS
- INS
- ACS

---

--------------------------------------------------
B. WEEKLY CHANGE
--------------------------------------------------

Delta_7D:
- current_value - value_7_days_ago

---

--------------------------------------------------
C. ACCELERATION (CRITICAL)
--------------------------------------------------

Acceleration:
(Delta_1D_today - Delta_1D_yesterday)

This detects:
- breakout ignition
- momentum spikes
- institutional entry waves

---

# 4. CREATE SIGNAL TREND STATES

For EACH signal classify trend:

- STRONGLY RISING (↑↑)
- RISING (↑)
- FLAT (→)
- FALLING (↓)
- STRONGLY FALLING (↓↓)

Apply to:
- VQS trend
- GVS trend
- COS trend
- INS trend
- ACS trend

---

# 5. CREATE NEW DASHBOARD TABLE

Add a new tab:

“Signal Movement Tracker”

Display per stock:

- VQS (current)
- VQS trend arrow
- VQS 7D change

- GVS (current)
- GVS trend arrow
- GVS 7D change

- COS (current)
- COS trend arrow
- COS 7D change

- INS (current)
- INS trend arrow
- INS 7D change

- ACS (current)
- ACS trend arrow
- ACS 7D change

Sort options:
- strongest INS increase
- strongest ACS increase
- strongest COS increase
- strongest acceleration

---

# 6. CREATE “EARLY BREAKOUT DETECTOR”

Flag stocks where:

--------------------------------------------------
EARLY BREAKOUT CONDITION
--------------------------------------------------

- INS is rising (↑ or ↑↑)
AND
- INS acceleration > threshold
AND
- ACS rising
AND
- COS still low or neutral (below 65)

Label:
“EARLY IGNITION SETUP”

Meaning:
Potential NBIS-style pre-breakout phase.

---

# 7. CREATE “TREND DIVERGENCE DETECTOR”

Flag when:

--------------------------------------------------
DIVERGENCE TYPE 1
--------------------------------------------------

INS rising
BUT COS falling

Label:
“SPECULATIVE MOMENTUM (UNCONFIRMED)”

---

--------------------------------------------------
DIVERGENCE TYPE 2
--------------------------------------------------

COS rising
BUT INS falling

Label:
“LATE CYCLE / EXHAUSTION RISK”

---

--------------------------------------------------
DIVERGENCE TYPE 3
--------------------------------------------------

ACS rising
INS rising
COS flat

Label:
“INSTITUTIONAL ACCUMULATION BEFORE REPRICING”

---

# 8. ADD MINI CHARTS (VERY IMPORTANT)

For each stock, show small trend lines (sparkline style):

- INS (7–30 day line)
- COS (7–30 day line)
- ACS (7–30 day line)

This is required so users can visually see acceleration.

---

# 9. PERFORMANCE REQUIREMENTS

- Do NOT recompute full history every second
- Cache signal snapshots
- Update only on refresh cycle (5–15 min or manual trigger)

---

# 10. FINAL GOAL

This feature should transform the system from:

“static scoring system”

TO:

“dynamic signal movement intelligence system”

The goal is to answer:

“What is improving fastest RIGHT NOW?”

NOT:

“What is the score today?”