Build a new feature for my existing stock signal system (VQS, GVS, COS, INS).

I want to add a new web page/dashboard tab called:

"INS Market Scanner"

This page should scan a large universe of stocks and return the TOP 10 highest INS scores for the current day.

---

# 1. CORE OBJECTIVE

The goal is to identify early-stage breakout stocks BEFORE they appear in COS rankings.

This scanner should function as:

> a real-time “opportunity radar” for NBIS / CRDO type moves

---

# 2. STOCK UNIVERSE

Create a scan universe that includes:

- S&P 500 stocks (default)
- NASDAQ 100 stocks
- Optionally: Russell 2000 subset (if available)

If full universe is not available, fallback to:
- top 300 by volume
- or API-provided watchlist universe

---

# 3. CALCULATION ENGINE

For each ticker:

Run existing pipeline:

1. Feature Engine
2. INS Score Calculation
3. VQS / GVS / COS (if available)

IMPORTANT:
- INS is the PRIMARY ranking metric
- COS is secondary validation only

---

# 4. OUTPUT REQUIREMENT (TOP 10 LIST)

Return a ranked table:

Columns:

- Rank (1–10)
- Ticker
- Company Name (if available)
- INS Score (primary sort)
- COS Score (secondary)
- GVS Score
- VQS Score
- INS Classification Label:
  - 80–100 = “EXPLOSIVE EARLY BREAKOUT”
  - 65–79 = “EARLY MOMENTUM BUILD”
  - 50–64 = “DEVELOPING”
  - <50 = “NO EDGE”

---

# 5. INS MARKET EDGE FILTERS (IMPORTANT)

Add a filter system that highlights:

## A. “PRE-COS BREAKOUT CANDIDATES”
Condition:
- INS > 70
- COS < 65

Label:
“EARLY OPPORTUNITY (pre-consensus)”

---

## B. “FALSE MOMENTUM RISK”
Condition:
- COS > 70
- INS falling or < 50

Label:
“LATE CYCLE / OVERHEATED”

---

## C. “HIGH-CONVICTION WINNERS”
Condition:
- INS > 75 AND COS > 70

Label:
“CRDO-TYPE COMPOUNDER SETUP”

---

# 6. UI / DASHBOARD REQUIREMENTS

Create a new tab at the top navigation:

Tabs:
- Dashboard (existing COS/VQS/GVS view)
- INS Scanner (NEW)

INS Scanner page should include:

### A. Top 10 table (main element)
Sortable by INS score

### B. Filters panel
- INS threshold slider (0–100)
- COS threshold slider
- Sector filter (if available)
- Market cap filter (small/mid/large)

### C. Color coding
- INS > 80 = green highlight
- 65–79 = yellow highlight
- <65 = gray

---

# 7. BONUS EDGE FEATURES (IMPORTANT — ADD THESE)

## Feature 1: “INS Momentum Change Tracker”
Track:

INS_today - INS_7d_ago

Display:

- Rising fast (green arrow)
- Flat (neutral)
- Falling (red arrow)

This helps detect NBIS-style pre-breakouts earlier than absolute score.

---

## Feature 2: “Divergence Alert”
If:

- INS rising
- COS still low

Label:
“EARLY IGNITION ZONE”

If:

- COS high
- INS declining

Label:
“EXHAUSTION WARNING”

---

## Feature 3: “Breakout Probability Score (NEW METRIC)”

Create a simplified probability model:

BreakoutScore =

0.5 * INS +
0.3 * INS_Momentum +
0.2 * Volume_Acceleration

Normalize to 0–100.

Display next to INS.

---

## Feature 4: “Watchlist Auto-Builder”

Automatically generate:

- Top 10 INS stocks → auto-added to watchlist tab
- Update daily or on refresh

---

# 8. PERFORMANCE REQUIREMENTS

- Must run efficiently (do NOT recompute full market every second)
- Cache results for at least 5–15 minutes
- Allow manual refresh button

---

# 9. FINAL GOAL

This system should behave like:

> a professional early-stage quant scanner that identifies breakout stocks BEFORE institutional consensus forms

INS is the primary ranking engine.
COS is secondary validation.

---

# 10. IMPORTANT DESIGN PRINCIPLE

COS = confirmation signal  
INS = discovery signal  

Never let COS override INS in ranking order.